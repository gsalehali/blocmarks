<% if @topic.destroyed? %>
  flashMsg( '#topic-' +<%= @topic.id %> ,"<%= flash[:notice] %>", "success")
  $('#topic-' +<%= @topic.id %>).next('hr').fadeOut(1000);
  $('#topic-' +<%= @topic.id %>).fadeOut(1000);
  <% flash.discard %>
<% else %>
  flashMsg("<%= flash[:error] %>", "error")
<% end %>
